% ======================================================
%  3 — ARCHITECTURE
% ======================================================
\section{System Architecture}

% --- 3.1 Pipeline overview ---
\begin{frame}{The Pipeline}
  \begin{center}
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[
      box/.style={draw=unisired, rounded corners=8pt, minimum width=2.6cm, minimum height=1.1cm,
                  align=center, font=\small\bfseries, text=unisired, fill=unisired!5, line width=1pt},
      human/.style={draw=unisired, rounded corners=8pt, minimum width=2.6cm, minimum height=1.1cm,
                  align=center, font=\small\bfseries, text=unisired, fill=unisired!15, line width=1pt,
                  dashed},
      arr/.style={->, >=Stealth, line width=1.5pt, unisired!60}
    ]
    \node[box] (text) at (0,0)    {Topic /\\Input Text};
    \node[box] (kw)   at (3.2,0)  {Keyword\\Extraction};
    \node[box] (clue) at (6.4,0)  {Clue\\Generation};
    \node[human](rev) at (9.6,0)  {Human\\Revision};
    \node[box] (grid) at (12.8,0) {Grid\\Assembly};
    \draw[arr] (text) -- (kw);
    \draw[arr] (kw)   -- (clue);
    \draw[arr] (clue) -- node[above, font=\scriptsize, text=graytext]{} (rev);
    \draw[arr] (rev)  -- (grid);
    \node[font=\scriptsize, text=graytext, below=0.45cm] at (kw)   {zero-shot LLM};
    \node[font=\scriptsize, text=graytext, below=0.45cm] at (clue) {few-shot LLM};
    \node[font=\scriptsize, text=graytext, below=0.45cm] at (grid) {greedy + scoring};
    \end{tikzpicture}}
  \end{center}
  \vfill
  {\small\color{graytext}\centering
   We also explored fine-tuning (Path B: keywords $\rightarrow$ clues directly) ---
   few-shot prompting with stronger models outperformed it.}
\end{frame}

% --- 3.2 Clue generation ---
\begin{frame}{Generating the Clues}
  \begin{columns}[c]
    \column{0.5\textwidth}
      \small
      \textbf{Step 1 — Keyword extraction} (zero-shot)\\[0.5em]
      LLM identifies pedagogically relevant terms:\\
      central to the topic, single word, suitable length.
      \vspace{0.8em}

      \textbf{Step 2 — Clue generation} (few-shot)\\[0.5em]
      Given text + keywords, LLM generates\\
      a crossword-style definition for each answer.
      \vspace{0.8em}

      {\scriptsize\color{graytext}\textit{``Photosynthesis converts sunlight\ldots''} $\rightarrow$ \textbf{clorofilla}\\
       Clue: \textit{``Pigmento che cattura la luce solare''}}

    \column{0.5\textwidth}
      \begin{center}
        \begin{tikzpicture}
          \fill[unisired]    (0,2.2) rectangle (4.0, 2.6);
          \node[font=\small\bfseries, text=white,    anchor=west] at (0.1, 2.4) {$\sim$80\% suitable keywords};
          \fill[unisired!60] (0,1.1) rectangle (3.1, 1.5);
          \node[font=\small\bfseries, text=white,    anchor=west] at (0.1, 1.3) {70--77\% acceptable clues};
          \fill[unisired!30] (0,0.0) rectangle (4.0, 0.4);
          \node[font=\small\bfseries, text=darktext, anchor=west] at (0.1, 0.2) {human revision → classroom-ready};
        \end{tikzpicture}
      \end{center}
  \end{columns}
\end{frame}

% --- 3.3 Grid generation ---
\begin{frame}{Building the Grid}
  \begin{columns}[c]
    \column{0.52\textwidth}
      \setlength{\fboxsep}{0.5em}
      \fcolorbox{unisired!40}{unisired!3}{\parbox{0.9\columnwidth}{%
        \scriptsize\ttfamily
        \textbf{\small\rmfamily Input:} clue--answer pairs\\[0.3em]
        place longest word at centre\\
        \textbf{while} stopping criteria not met \textbf{do}\\
        \hspace{1em}\textbf{if} all words placed \textbf{then}\\
        \hspace{2em}compute Score\\
        \hspace{2em}\textbf{if} Score $>$ best \textbf{then} save grid\\
        \hspace{1em}\textbf{else}\\
        \hspace{2em}find intersections for next word\\
        \hspace{2em}\textbf{if} placement found \textbf{then}\\
        \hspace{3em}place word in grid\\
        \hspace{2em}\textbf{else} backtrack, try alternative\\
        \textbf{end while}\\[0.3em]
        \textbf{\small\rmfamily Output:} best grid + clue-position map%
      }}
    \column{0.48\textwidth}
      \centering
      \vspace{0.5em}
      \textbf{Scoring function:}\\[0.6em]
      $\text{Score} = (FW + 0.5 \cdot LL) \times FR \times LR$\\[0.8em]
      {\small
      \begin{tabular}{@{}ll@{}}
        $FW$ & filled words\\
        $LL$ & linked (shared) letters\\
        $FR$ & filled ratio (compactness)\\
        $LR$ & linked letter ratio\\
      \end{tabular}}\\[0.8em]
      \includegraphics[width=0.72\textwidth, height=0.38\textheight, keepaspectratio]{figures/crossword.pdf}
  \end{columns}
\end{frame}